# bert-variants-nlp-classification
Exploration of NLP classification tasks using BERT and its variants (DistilBERT, multilingual BERT), including experiments with different classifier architectures (e.g, single vs multi-layer), in-domain and out-of-domain sentiment classification (IMDB → Amazon reviews), and analysis of model performance, generalization, and sensitivity to data.
